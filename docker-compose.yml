services:
  nginx:
    container_name: "Webapp"
    image: nginx:latest
    ports:
      - "8080:80"
    volumes:
      - ./src/public:/var/www/html
      - ./docker/nginx.conf:/etc/nginx/conf.d/default.conf

